version: '3.8'

services:
  discord-bot:
    build: .
    container_name: 5ive-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./src:/app/src:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
