## 2024-10-24 - Keyboard Shortcut Discoverability
**Learning:** Adding visual `<kbd>` hints in helper text effectively teaches power users about shortcuts without cluttering the primary UI or requiring a separate "shortcuts" modal.
**Action:** When implementing shortcuts, always pair `aria-keyshortcuts` on the interactive element with a visible `<kbd>` hint in the nearby helper text.
