## 2024-05-24 - [Keyboard Shortcuts Pattern]
**Learning:** Keyboard shortcuts for actions should be implemented with `aria-keyshortcuts` on the button and visual `<kbd>` hints in helper text, supporting both `Ctrl` and `Cmd` for cross-platform consistency.
**Action:** Use this pattern for all future shortcut implementations to ensure accessibility and discoverability.
